## What this changes and why

<!-- The "why" matters more than the "what" — the diff already shows what changed. -->

## RFC or issue

<!-- Link the RFC (rfcs/) or issue this implements, if any. Small, non-user-visible
     changes (bug fixes, refactors, docs) don't need one — see CONTRIBUTING.md. -->

## Checklist

- [ ] `make test` passes
- [ ] `make generate manifests` produces no uncommitted diff (only if `api/v1` changed)
- [ ] Commits are signed off (`git commit -s`) — see [CONTRIBUTING.md](../CONTRIBUTING.md#the-dco-sign-off-every-commit)
- [ ] Docs updated if this changes user-facing behavior (`docs/`, `README.md`)

## Anything reviewers should look at closely

<!-- Optional: a tricky invariant, a tradeoff you're unsure about, a spot you want a
     second pair of eyes on. -->
